---
interface Props {
  category: string;
  skills: string[];
}

const { category, skills } = Astro.props;
---

<div class="p-4 bg-gray-50 rounded-lg hover:bg-gray-100 transition-colors">
  <h3 class="font-semibold mb-2 text-gray-800">{category}</h3>
  <p class="text-gray-600 text-sm leading-relaxed">{skills.join(" • ")}</p>
</div>